define("style/font", function(requirejs, module){    var me = QuickFoxNotes;    return {        getFontFamily: function (index) {            try {                return window.getComputedStyle(me.getEditor(index), null)                    .getPropertyValue('font-family')                    .match(/[^\"]+/).toString();    //In Linux name comes with qoutations            }            catch (e) {                return "";            }        }    }});