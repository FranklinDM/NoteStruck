define("console/log", function(requirejs, module){    function isNumber (o) {        return !isNaN (o-0);    }    var msgs = {        1: "initialized.",        2: "Unknown command passed to QFN!",    }    return function(msg, additions) {        const prefix = "QuickFox Notes [" + requirejs("misc/time")() + "]: ";        qfnServices.jsDump.logStringMessage(            prefix +            (isNumber(msg) ? (msgs[msg] + (additions ? ("\n" + additions) : "")) : msg)        );    }})